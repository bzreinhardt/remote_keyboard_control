remote_keyboard_control is a python script and arduino software to allow PWM control and data logging from an arduino connected via a bluetooth or other wireless serial connection (like an xbee)

Usage: compile and load the .ino file to the arduino and run the python script from the main computer. 
